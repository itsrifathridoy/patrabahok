"use client";

export default function FolderIndexPage() {
  return (
    <div className="flex-1 flex items-center justify-center text-sm" style={{ color: "var(--text-muted)" }}>
      Select a message to read it.
    </div>
  );
}
